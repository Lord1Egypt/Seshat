# patterns/

Detection patterns live here (Phase 2). Each pattern is a YAML file plus
labeled fixtures (≥1 true-positive, ≥1 clean) under `fixtures/`.

Empty for now — Phase 0 lays the foundation; the catalog is grown in Phase 2.
See [../docs/PATTERNS.md](../docs/PATTERNS.md) for the format and taxonomy.
