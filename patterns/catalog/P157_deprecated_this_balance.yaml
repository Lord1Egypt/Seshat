id: P157
name: 'Deprecated this.balance'
severity: low
category: BYTECODE_LOWLEVEL
confidence: 0.35
swc: SWC-000
cwe: CWE-477
detectors:
  - type: regex
    pattern: '\bthis\.balance\b'
    confidence: 0.35
    description: 'Deprecated this.balance (use address(this).balance)'
tests:
  positive:
    - 'contract C { function f() public view returns (uint) { return this.balance; } }'
  negative:
    - 'contract Safe { uint256 private value; function getValue() external view returns (uint256) { return value; } }'
