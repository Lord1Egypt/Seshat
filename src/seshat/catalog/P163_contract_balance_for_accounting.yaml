id: P163
name: 'Contract Balance For Accounting'
severity: low
category: BUSINESS_LOGIC
confidence: 0.3
swc: SWC-000
cwe: CWE-682
detectors:
  - type: regex
    pattern: 'address\(this\)\.balance'
    confidence: 0.3
    description: 'Using live contract balance for accounting'
tests:
  positive:
    - 'contract C { function f() public view returns (uint) { return address(this).balance; } }'
  negative:
    - 'contract Safe { uint256 private value; function getValue() external view returns (uint256) { return value; } }'
