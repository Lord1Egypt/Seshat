id: P099
name: 'tx.gasprice Dependence'
severity: low
category: BUSINESS_LOGIC
confidence: 0.35
swc: SWC-000
cwe: CWE-330
detectors:
  - type: regex
    pattern: 'tx\.gasprice'
    confidence: 0.35
    description: 'Logic depends on tx.gasprice'
tests:
  positive:
    - 'contract C { function f() public view returns (uint) { return tx.gasprice; } }'
  negative:
    - 'contract Safe { uint256 private value; function getValue() external view returns (uint256) { return value; } }'
